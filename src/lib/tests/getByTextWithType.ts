import React from "react"
import { ReactTestInstance } from "react-test-renderer"

export const getByTextWithType = <P>(text: string | RegExp, type: React.ComponentType<P>): ReactTestInstance => {
  return 3
}
